<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" asp-controller="BlogPost" asp-action="Index">Murky Pulse</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                <!-- Home link that goes to BlogPost/Index -->
                <li class="nav-item">
                    <a class="nav-link active" asp-controller="BlogPost" asp-action="Index">Home</a>
                </li>

                

                @if (User.IsInRole("Admin"))
                {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="BlogCategory" asp-action="Index">Categories</a>
                    </li>
                }
            </ul>

            <!-- Authentication section remains the same -->
            @if (User.Identity.IsAuthenticated)
            {
                <div class="ms-auto me-5">
                    <a asp-controller="Auth" asp-action="SignOut" class="btn btn-outline-light">SignOut</a>
                    
                </div>
            }
            else
            {
                <div class="d-flex ms-auto gap-2 me-5">
                    <a asp-controller="Auth" asp-action="SignIn" class="btn btn-outline-light">SignIn</a>
                    <a asp-controller="Auth" asp-action="SignUp" class="btn btn-outline-light">SignUp</a>
                </div>
            }
        </div>
    </div>
</nav>